fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
sqlalchemy==2.0.35
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
python-ulid==3.0.0
livekit-api>=1.0,<2.0
httpx==0.27.2
redis==5.0.8
apscheduler==3.10.4
python-multipart==0.0.10
# Rendering the "What's up next" slide (broadcast-style rundown shown
# before any playlist item > 5 min when the toggle is on).
pillow==12.2.0
numpy==2.1.3
pyotp==2.9.0
pytest==8.3.3
pytest-asyncio==0.24.0

# YouTube manual upload — keep deps minimal: httpx already used; we hand-roll
# the OAuth + resumable-upload flow rather than pulling google-api-python-client.
